# 五行八卦推理规则库 · 自我演化机制 · 详细设计 v1.0

> 本文档是《五行八卦推理规则库·独立提案 v1.0》第四章的细化扩展。

---

## 一、演化生命周期总览

```
┌─────────────────────────────────────────────────────────────┐
│                    浏览器内闭环（零网络）                       │
│                                                             │
│  ┌──────┐    ┌──────┐    ┌──────┐    ┌──────┐              │
│  │ 捕获  │ →  │ 积累  │ →  │ 适应  │ →  │ 报告  │              │
│  │Capture│    │Accum.│    │Adapt │    │Report│              │
│  └──────┘    └──────┘    └──────┘    └──────┘              │
│       ↓           ↓           ↓           ↓                │
│  用户修改     localStorage   推断时优先    导出JSON文件         │
│  → 记录差异    → 频率统计    读取修正     → 含合并建议         │
│                                                             │
└──────────────────────────┬──────────────────────────────────┘
                           ↓ 用户主动操作
┌──────────────────────────────────────────────────────────────┐
│  ┌──────┐    ┌──────┐    ┌──────┐                          │
│  │ 审核  │ →  │ 合并  │ →  │ 重建  │   需要用户手动完成        │
│  │Review│    │Merge │    │Rebuild│                          │
│  └──────┘    └──────┘    └──────┘                          │
└──────────────────────────────────────────────────────────────┘
```

**五阶段说明：**

| 阶段 | 触发方式 | 执行位置 | 是否需要网络 |
|:----:|:--------|:--------|:---------:|
| 捕获 | 用户在管理面板修改属性 | 浏览器 JS | 否 |
| 积累 | 每次页面加载时自动执行 | 浏览器 JS | 否 |
| 适应 | 每次推断 wuxing/bagua 时自动执行 | 浏览器 JS | 否 |
| 报告 | 用户点击"导出修正报告"按钮 | 浏览器 JS → 下载文件 | 否（生成文件在本地） |
| 审核+合并+重建 | 用户下载报告 → 编辑 → 合并 → 重新构建 | 用户手动 + Python | 否 |

---

## 二、阶段一：捕获（Capture）

### 2.1 触发时机

当用户在管理面板中编辑概念并保存时，执行以下逻辑：

```javascript
function saveConcept(concept) {
    var old_wuxing = concept.wuxing || '';
    var old_bagua = concept.bagua || '';
    var new_wuxing = editForm.wuxing.value;
    var new_bagua = editForm.bagua.value;

    // 保存概念（已有逻辑）
    saveData();

    // 捕获修正（新增逻辑）
    if (new_wuxing !== old_wuxing || new_bagua !== old_bagua) {
        captureCorrection(concept, old_wuxing, old_bagua, new_wuxing, new_bagua);
    }
}
```

### 2.2 修正记录数据结构

每条修正记录包含完整的上下文信息：

```javascript
var correction = {
    id: 'c_' + Date.now() + '_' + Math.random().toString(36).substr(2, 4),
    concept: {
        label: '熏习',
        desc: '现行反复熏习种子...',
        layer: 'outer',          // outer/middle/inner
        ringIndex: 0             // 第几层
    },
    change: {
        field: 'wuxing',         // 'wuxing' 或 'bagua' 或 'both'
        from: '金',
        to: '水'
    },
    inference: {
        inferred_wuxing: '金',    // 规则引擎推断的原始值
        inferred_bagua: '兑',
        confidence: 0.6,         // 推断置信度（0-1）
        matched_keywords: ['收敛', '回归'],  // 命中的关键词
        score_detail: { 木: 1, 火: 0, 土: 2, 金: 5, 水: 3 }  // 各五行得分
    },
    context: {
        domain: 'buddhism',       // 领域（从页面标题推断或用户设定）
        timestamp: '2026-07-23T10:30:00Z',
        session_count: 3          // 当前会话的第几次修正（用于区分批量vs逐个）
    },
    status: 'pending_review'      // pending_review / accepted / rejected
};
```

### 2.3 存储到 localStorage

```javascript
function captureCorrection(concept, old_wx, old_bg, new_wx, new_bg) {
    var corrections = loadCorrections();
    var record = buildCorrectionRecord(concept, old_wx, old_bg, new_wx, new_bg);
    corrections.history.push(record);
    // 同时更新关键词频率统计
    updateKeywordStats(corrections, record);
    saveCorrections(corrections);
}

function loadCorrections() {
    try {
        var data = localStorage.getItem(STORAGE_KEY + '_corrections');
        return data ? JSON.parse(data) : { history: [], keyword_stats: {}, exact_map: {} };
    } catch(e) {
        return { history: [], keyword_stats: {}, exact_map: {} };
    }
}

function saveCorrections(corrections) {
    try {
        localStorage.setItem(STORAGE_KEY + '_corrections', JSON.stringify(corrections));
    } catch(e) { /* localStorage 满，清理旧记录后重试 */ }
}
```

### 2.4 localStorage 存储结构

```
{STORAGE_KEY}_corrections = {
    history: [                    // 全部修正记录（最多保留 500 条）
        { id, concept, change, inference, context, status },
        ...
    ],
    keyword_stats: {              // 关键词 → 五行频率统计
        "熏习":  { "水": 3, "金": 1 },
        "收敛":  { "金": 5, "木": 1 },
        "承载":  { "土": 8 },
        ...
    },
    exact_map: {                  // 精确匹配：概念名 → 用户确认的五行
        "熏习":  { wuxing: "水", bagua: "坎", count: 3 },
        "阿赖耶识": { wuxing: "土", bagua: "坤", count: 5 },
        ...
    },
    meta: {
        version: "1.0",
        created: "2026-07-23T10:00:00Z",
        last_modified: "2026-07-23T15:30:00Z",
        total_corrections: 47,
        domains: ["buddhism", "chinese_medicine"]
    }
}
```

### 2.5 存储容量管理

| 策略 | 阈值 | 动作 |
|:-----|:----:|:-----|
| 记录上限 | 500 条 | 超过后删除最早的 `pending_review` 记录 |
| 关键词统计上限 | 200 个关键词 | 超过后删除频率最低的 20% |
| 总存储上限 | 4MB（localStorage 限制通常 5MB） | 触发时提示用户导出并清理 |

---

## 三、阶段二：积累（Accumulate）

### 3.1 页面加载时的统计计算

每次页面加载时，从 `history` 重建统计表：

```javascript
function rebuildStats(corrections) {
    var stats = { keyword_stats: {}, exact_map: {}, concept_types: {} };

    corrections.history.forEach(function(record) {
        if (record.status === 'rejected') return; // 跳过被否决的修正

        var label = record.concept.label;
        var newWuxing = record.change.to;

        // 精确匹配更新
        if (!stats.exact_map[label]) {
            stats.exact_map[label] = { wuxing: newWuxing, bagua: '', count: 0 };
        }
        stats.exact_map[label].wuxing = newWuxing;
        stats.exact_map[label].count++;

        // 如果 bagua 也被修正
        if (record.change.field === 'bagua' || record.change.field === 'both') {
            stats.exact_map[label].bagua = record.change.field === 'both' ?
                record.change.to_bagua : newWuxing;
        }

        // 关键词频率更新
        var keywords = extractKeywords(label + ' ' + record.concept.desc);
        keywords.forEach(function(kw) {
            if (!stats.keyword_stats[kw]) stats.keyword_stats[kw] = {};
            if (!stats.keyword_stats[kw][newWuxing]) stats.keyword_stats[kw][newWuxing] = 0;
            stats.keyword_stats[kw][newWuxing]++;
        });
    });

    return stats;
}
```

### 3.2 关键词提取

从概念标题和描述中提取有意义的关键词：

```javascript
function extractKeywords(text) {
    // 分词策略：2-4字的中文词组 + 英文单词
    var words = [];
    // 中文：提取 2-4 字连续中文字符
    var cnMatches = text.match(/[\u4e00-\u9fff]{2,4}/g) || [];
    words = words.concat(cnMatches);
    // 英文：提取独立单词（≥3字母）
    var enMatches = text.match(/[a-zA-Z]{3,}/g) || [];
    words = words.concat(enMatches.map(function(w) { return w.toLowerCase(); }));
    // 去重
    return unique(words);
}
```

### 3.3 频率统计的加权计算

不是简单的计数，而是考虑时间衰减：

```javascript
function getKeywordWeight(keyword, targetWuxing) {
    var stats = loadCorrections().keyword_stats;
    var entries = stats[keyword] || {};
    var targetCount = entries[targetWuxing] || 0;
    var totalCount = 0;
    for (var wx in entries) { totalCount += entries[wx]; }

    if (totalCount === 0) return 0;

    // 频率 = 目标五行次数 / 总次数
    // 置信度 = 总次数越高越可信（最少 2 次才有意义）
    var frequency = targetCount / totalCount;
    var confidence = Math.min(1, totalCount / 5);  // 5次达到满置信度
    return frequency * confidence;
}
```

### 3.4 统计结果可视化

在管理面板中可查看当前统计（可选 UI）：

```
┌─────────────────────────────────────┐
│ 规则学习统计                         │
│ 总修正次数: 47                       │
│ 精确匹配: 12 个概念                  │
│ 关键词学习: 34 个                    │
│                                     │
│ 高置信度调整 (>0.7):                │
│   "熏习" → 水 (5/6, 置信度 1.0)    │
│   "承载" → 土 (8/8, 置信度 1.0)    │
│   "明照" → 火 (4/5, 置信度 0.8)    │
│                                     │
│ 中置信度调整 (0.3-0.7):             │
│   "转化" → 水 (3/5, 置信度 0.6)    │
│   "突破" → 木 (2/4, 置信度 0.4)    │
│                                     │
│ [导出修正报告]  [清空修正记录]       │
└─────────────────────────────────────┘
```

---

## 四、阶段三：适应（Adapt）

### 4.1 推断优先级链

当需要为一个概念推断五行时，按以下优先级：

```
优先级 1（最高）：精确匹配
  → exact_map 中有该概念名 且 count >= 2
  → 直接返回用户确认的值

优先级 2：关键词频率
  → 从概念文本中提取关键词
  → 查询 keyword_stats，找到频率最高的五行
  → 权重 >= 0.5 时采用

优先级 3（最低）：基础规则
  → 使用 wuxing_bagua_rules.json 中的关键词匹配
  → 这是默认行为，与没有演化系统时相同
```

### 4.2 推断算法

```javascript
function inferWuxing(label, desc) {
    var corrections = loadCorrections();

    // 优先级 1：精确匹配
    var exact = corrections.exact_map[label];
    if (exact && exact.count >= 2) {
        return {
            wuxing: exact.wuxing,
            source: 'exact_match',
            confidence: Math.min(1, exact.count / 5)
        };
    }

    // 优先级 2：关键词频率
    var text = label + ' ' + (desc || '');
    var keywords = extractKeywords(text);
    var wuxingScores = {};
    keywords.forEach(function(kw) {
        var stats = corrections.keyword_stats[kw] || {};
        for (var wx in stats) {
            var weight = getKeywordWeight(kw, wx);
            if (weight > 0.5) {  // 只采信高置信度
                wuxingScores[wx] = (wuxingScores[wx] || 0) + weight;
            }
        }
    });
    var bestWx = argmax(wuxingScores);
    if (bestWx && wuxingScores[bestWx] > 1.5) {
        return {
            wuxing: bestWx,
            source: 'learned_keyword',
            confidence: Math.min(0.9, wuxingScores[bestWx] / 5)
        };
    }

    // 优先级 3：基础规则（无修正数据或置信度不足）
    return inferWuxingFromBaseRules(label, desc);
}
```

### 4.3 八卦推断的修正感知

八卦推断也受益于修正数据：

```javascript
function inferBagua(label, desc, wuxing) {
    // 1. 精确匹配
    var corrections = loadCorrections();
    var exact = corrections.exact_map[label];
    if (exact && exact.bagua && exact.count >= 2) {
        return { bagua: exact.bagua, source: 'exact_match' };
    }

    // 2. 从修正数据中学习：该五行下用户最常选哪个卦
    var baguaPreference = {};
    (corrections.history || []).forEach(function(r) {
        if (r.status === 'rejected') return;
        if (r.concept.label !== label) return;
        if (r.change.field === 'bagua' || r.change.field === 'both') {
            var bg = r.change.field === 'both' ? r.change.to_bagua : r.change.to;
            baguaPreference[bg] = (baguaPreference[bg] || 0) + 1;
        }
    });
    var bestBg = argmax(baguaPreference);
    if (bestBg && baguaPreference[bestBg] >= 2) {
        return { bagua: bestBg, source: 'learned_preference' };
    }

    // 3. 基础规则
    return inferBaguaFromBaseRules(label, desc, wuxing);
}
```

### 4.4 用户反馈闭环

修正记录有三种状态，用户可以更改：

| 状态 | 含义 | 推断时的行为 |
|:----:|:-----|:----------|
| `pending_review` | 刚记录，未确认 | 参与统计但不提升精确匹配优先级 |
| `accepted` | 用户明确确认正确 | 完全信任，精确匹配优先级 1 |
| `rejected` | 用户撤销了这次修正 | 从统计中排除 |

管理面板中，每次修正后弹出轻量提示：

```
┌──────────────────────────────────────────┐
│ 已记录修正：熏习 金→水                    │
│                                          │
│ 此修正将用于改进后续的五行推断。           │
│ [确认正确] [撤销] [忽略]                   │
└──────────────────────────────────────────┘
```

---

## 五、阶段四：报告（Report）

### 5.1 导出触发

管理面板新增"导出修正报告"按钮，位于统计面板底部。

### 5.2 报告 JSON 结构

```json
{
    "report_version": "1.0",
    "generated_at": "2026-07-23T16:00:00Z",
    "source": {
        "title": "唯识论 · 种子现行 · v2.0",
        "storage_key": "mobius_c3f10b6e",
        "total_concepts": 23,
        "total_corrections": 47,
        "accepted_corrections": 38,
        "rejected_corrections": 3,
        "pending_corrections": 6
    },
    "summary": {
        "exact_matches": 12,
        "keyword_adjustments": [
            {
                "keyword": "熏习",
                "suggested_wuxing": "水",
                "current_rules_wuxing": "金",
                "frequency": "5/6",
                "confidence": 1.0,
                "reason": "用户 5 次将含'熏习'的概念修正为水，原始规则推断为金"
            },
            {
                "keyword": "明照",
                "suggested_wuxing": "火",
                "current_rules_wuxing": null,
                "frequency": "4/5",
                "confidence": 0.8,
                "reason": "新增关键词建议：'明照'→火"
            }
        ],
        "bagua_adjustments": [
            {
                "wuxing": "水",
                "preferred_bagua": "坎",
                "current_bagua_options": ["坎"],
                "count": 8,
                "reason": "水五行下用户一致选择坎卦"
            }
        ]
    },
    "suggested_rules_patch": {
        "wuxing": {
            "水": {
                "keywords_add": ["熏习", "潜流", "暗动"],
                "keywords_remove": [],
                "weight_change": { "from": 1.0, "to": 1.3, "reason": "水五行被频繁修正选中" }
            },
            "金": {
                "keywords_add": [],
                "keywords_remove": ["熏习"],
                "weight_change": { "from": 1.0, "to": 0.8, "reason": "金五行中'熏习'被5次修正为水" }
            }
        },
        "bagua": {},
        "layer_assignment": {}
    },
    "quality_metrics": {
        "correction_rate": 0.47,
        "avg_confidence_of_accepted": 0.82,
        "most_corrected_ring": "outer",
        "least_corrected_ring": "inner",
        "consistency_score": 0.91
    },
    "full_history": [
        // 完整的 corrections.history 数组
    ]
}
```

### 5.3 报告内容说明

| 区块 | 内容 | 用途 |
|:-----|:-----|:-----|
| `source` | 来源信息 | 确认报告来自哪个数据集 |
| `summary.keyword_adjustments` | 关键词调整建议 | 直接合并到规则库 |
| `summary.bagua_adjustments` | 八卦偏好建议 | 更新卦德匹配权重 |
| `suggested_rules_patch` | 结构化的规则补丁 | **可以直接 diff 合并到 wuxing_bagua_rules.json** |
| `quality_metrics` | 质量指标 | 评估推断规则的准确度 |
| `full_history` | 完整原始记录 | 审计和回溯 |

### 5.4 `suggested_rules_patch` 的合并方式

报告中的 `suggested_rules_patch` 设计为可以直接用于规则库更新：

```python
# merge_patch.py（独立小工具，未来实现）
import json

def merge_patch(rules_path, report_path, output_path):
    rules = json.load(open(rules_path))
    report = json.load(open(report_path))
    patch = report['suggested_rules_patch']

    # 合并关键词添加
    for wx, changes in patch.get('wuxing', {}).items():
        if wx in rules['wuxing']:
            for kw in changes.get('keywords_add', []):
                if kw not in rules['wuxing'][wx]['keywords']:
                    rules['wuxing'][wx]['keywords'].append(kw)
            for kw in changes.get('keywords_remove', []):
                if kw in rules['wuxing'][wx]['keywords']:
                    rules['wuxing'][wx]['keywords'].remove(kw)
            if 'weight_change' in changes:
                rules['wuxing'][wx]['weight'] = changes['weight_change']['to']

    rules['version'] = increment_version(rules['version'])
    rules['updated'] = report['generated_at']

    json.dump(rules, open(output_path, 'w'), ensure_ascii=False, indent=2)
    print(f"合并完成: {len(patch.get('wuxing', {}))} 个五行更新")
```

---

## 六、阶段五：审核+合并+重建

### 6.1 完整工作流

```
1. 用户在多个数据集的 HTML 页面中持续修正概念属性
       ↓
2. 积累足够的修正记录后，点击"导出修正报告"
       ↓ （下载 corrections_report_2026-07-23.json）
3. 用户审查报告中的 suggested_rules_patch
   - 确认关键词添加/删除是否合理
   - 调整权重变化是否适当
   - 可以手动编辑 JSON
       ↓
4. 运行合并工具（或手动编辑 rules.json）
   python merge_patch.py rules/wuxing_bagua_rules.json corrections_report.json rules_updated.json
       ↓
5. 将更新后的规则文件替换原文件
       ↓
6. 重新构建所有 HTML
   python build_mobius.py --data ... --title ... --output ...
       ↓
7. 新构建的 HTML 内嵌了更准确的规则，初始推断质量提升
```

### 6.2 迭代效果

| 迭代 | 精确匹配数 | 关键词学习数 | 推断准确率（估） |
|:----:|:---------:|:---------:|:-------------:|
| v0（无演化） | 0 | 0 | ~65% |
| v1（首次导出） | 12 | 8 | ~78% |
| v2（二次导出） | 18 | 15 | ~85% |
| v3（三次导出） | 22 | 20 | ~90% |

> 推断准确率 = 用户未修改的概念数 / 总概念数

---

## 七、跨数据集知识迁移

### 7.1 问题场景

用户在唯识论数据集中修正了"熏习→水"，后来创建道德经数据集时，"熏习"这个概念也应该推断为水。

### 7.2 解决方案：全局修正池

在 localStorage 中维护一个全局修正池（与数据集无关）：

```
{STORAGE_KEY_GLOBAL}_corrections_global = {
    keyword_stats: {
        "熏习":  { "水": 8, "金": 2 },    // 跨所有数据集的累计
        "承载":  { "土": 12 },
        ...
    },
    exact_map: {},                        // 精确匹配不跨数据集（同名概念可能含义不同）
    source_domains: {
        "buddhism": 35,
        "chinese_medicine": 12
    }
}
```

### 7.3 跨域推断策略

```
推断某概念的五行时：
    1. 先查本数据集的精确匹配（数据集内优先）
    2. 再查本数据集的关键词频率
    3. 再查全局关键词频率（降权 50%，因为不同领域含义可能不同）
    4. 最后查基础规则
```

### 7.4 全局池的管理

| 操作 | 触发方式 |
|:-----|:--------|
| 写入全局池 | 每次修正时自动同步（用户可在设置中关闭） |
| 读取全局池 | 推断时自动使用 |
| 清空全局池 | 管理面板中"清空全局修正数据" |
| 导出全局池 | "导出全局修正报告"按钮 |

---

## 八、冲突处理

### 8.1 同一关键词指向不同五行

场景：用户在数据集 A 中将"转化"修正为水，在数据集 B 中修正为木。

处理策略：

```javascript
function resolveConflict(keyword, wuxingA, countA, wuxingB, countB, domainA, domainB) {
    // 策略 1：领域内优先
    if (domainA === currentDomain) return wuxingA;
    if (domainB === currentDomain) return wuxingB;

    // 策略 2：频率优先
    if (countA > countB + 2) return wuxingA;
    if (countB > countA + 2) return wuxingB;

    // 策略 3：标记为冲突，不采信
    return null;  // 回退到基础规则
}
```

### 8.2 用户反复修改同一概念

场景：用户今天把"熏习"改为水，明天又改回金，后天又改为水。

处理策略：

```javascript
// 只记录最近 3 次修正的走向
// 如果 3 次中有 2 次指向同一五行，才确认为精确匹配
// 否则标记为 unstable，不参与学习
function isStableCorrection(history, label) {
    var recent = history
        .filter(r => r.concept.label === label)
        .slice(-3)  // 最近 3 次
        .map(r => r.change.to);

    if (recent.length < 2) return false;

    var counts = {};
    recent.forEach(wx => { counts[wx] = (counts[wx] || 0) + 1; });
    var max = Math.max.apply(null, Object.values(counts));
    return max >= 2;  // 2/3 以上一致才视为稳定
}
```

### 8.3 修正与基础规则严重矛盾

场景：基础规则明确"肝→木"（中医领域精确映射），但用户修正为"肝→火"。

处理策略：

```
如果修正目标的置信度 < 基础规则的 fixed_mapping 置信度：
    → 在报告中标注为"与固定规则冲突，需人工审核"
    → 不自动合并到规则库
    → 但仍然在当前浏览器的推断中采用用户版本
```

---

## 九、安全性

### 9.1 数据隔离

| 数据 | 隔离方式 | 原因 |
|:-----|:--------|:-----|
| 概念数据 | `{STORAGE_KEY}` 按数据集隔离 | 不同数据集的概念互不干扰 |
| 修正记录 | `{STORAGE_KEY}_corrections` 按数据集隔离 | 修正上下文属于特定数据集 |
| 全局修正池 | `mobius_corrections_global` 全局共享 | 跨数据集知识迁移 |
| 音频偏好 | `{STORAGE_KEY}_audio_state` 按数据集隔离 | 不同场景音量不同 |

### 9.2 数据清除

管理面板提供分层清除：

- "清空本次修正" → 只清当前会话的 pending 记录
- "重置所有修正" → 清空该数据集的全部修正记录
- "清空全局修正" → 清空全局修正池

每次清除前需确认（alert 确认框）。

### 9.3 导入导出

| 操作 | 格式 | 用途 |
|:-----|:-----|:-----|
| 导出修正报告 | JSON | 合并到规则库 |
| 导出概念数据（已有） | JSON | 备份和迁移 |
| 导入修正记录 | JSON | 从其他设备恢复 |
| 导入概念数据（已有） | JSON | 恢复和覆盖 |

---

## 十、与 template.html 的集成规范

### 10.1 内嵌规则数据

`build_mobius.py` 可以选择将精简版规则内嵌到 HTML 中（用于纯离线推断）：

```python
# --embed-rules 参数
if args.embed_rules:
    rules = load_rules(args.rules)
    # 精简：只嵌入 keywords，不嵌入 domain overrides
    minimal_rules = {
        wuxing: { wx: {'keywords': data['keywords']} for wx, data in rules['wuxing'].items() },
        bagua: { bg: {'wuxing': data['wuxing'], 'concept_types': data['concept_types']}
                 for bg, data in rules['bagua'].items() }
    }
    html = html.replace('/*WUXING_RULES_PLACEHOLDER*/',
                        'var WUXING_RULES = ' + json.dumps(minimal_rules, ensure_ascii=False) + ';')
```

### 10.2 管理面板 UI 变更

在现有 CRUD 管理面板中增加：

| 位置 | 新增元素 |
|:-----|:--------|
| 编辑表单 | wuxing 下拉选择器（木/火/土/金/水） |
| 编辑表单 | bagua 下拉选择器（☰乾/☱兑/.../☷坤） |
| 编辑表单 | 推断结果显示（当前规则推断值 vs 用户选择值） |
| 管理面板顶部 | "规则学习统计" 折叠面板 |
| 管理面板底部 | "导出修正报告" + "导入修正记录" 按钮 |

---

> 整理时间：2026-07-23
> 框架：五行八卦推理规则库 v1.0 · 自我演化机制
