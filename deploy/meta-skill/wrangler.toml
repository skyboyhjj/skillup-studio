# Cloudflare Pages + Worker 配置
# meta-skill.org/studio/ — 体验展示

name = "meta-skill-studio"
main = "worker.js"
compatibility_date = "2025-01-01"

# 静态站点配置
[site]
bucket = "../../frontend/studio"

# 路由规则
[[routes]]
pattern = "meta-skill.org/*"
zone_name = "meta-skill.org"

[env.production]
# Worker 环境变量
[env.production.vars]
BACKEND_URL = "http://121.41.215.36:8000"