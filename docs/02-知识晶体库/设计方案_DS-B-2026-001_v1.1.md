# 体证记录回流 · 完整设计方案（B-1/B-2）v1.1

**文档编号**: DS-B-2026-001
**版本**: v1.1（合并审核报告全部意见，2026-08-12）
**日期**: 2026-08-12
**依据**: PRP-2026-008（镜鉴反馈库评估）+ DEC-2026-009（四维五行定案）+ IF-2026-006（晶体契约）+ SOP Step 7（反馈库模板设计）+ 审核报告（DS-B-2026-001 审核，有条件通过 → 4 建议 + 3 发现 + 1 确认，全部采纳）
**状态**: 定稿（可实施）
**定位**: S3 体证卡片（TizhengCard）生命周期的闭环——draft → completed → 公共智慧池

---

## 零、审核意见采纳清单（v1.0 → v1.1）

| # | 意见 | 采纳 | 落点 |
|---|------|:----:|------|
| A1 | VALID_TYPES 抽取共享契约模块（实测 5 处硬编码，比审核 3 处更多） | ✅ | §八 8.2 新增 `contracts.py` |
| A2 | 护栏 1 显式标注扫描边界（模式 vs 上下文推断） | ✅ | §4.2 护栏 1 |
| A3 | 护栏 2 失败提示"请补充具体情境"（建设性指引） | ✅ | §4.2 护栏 2 |
| A4 | 池条目 `x-evidence.anonymization: best-effort` 字段 + S4 检索端标注 | ✅ | §5.2 + §6 |
| A5 | 池条目 `x-evidence.depth`（复用护栏 2 数据，零新增检查） | ✅ | §5.2 + §6 |
| A6 | x-application 回注持久化——**选方案 A**（回注数据库） | ✅ | §七 |
| A7 | 状态机统一三级撤回（completed→draft；published→completed，不直接回 draft） | ✅ | §2.2 |
| A8 | 池条目 ID 改时间戳+短哈希（防碰撞） | ✅ | §5.1 |
| A9 | §5.2 明确"摘要为引用性精炼，非原文复制"（契约引用不复制） | ✅ | §5.2 |

---

## 一、背景与目标

### 1.1 现状

S3 已交付 TizhengCard 生成器（`build_tizheng_card.py`），卡片以 `status: draft` 生成，正文含"体证记录（证）"三问占位（我做到了什么 + 我的体验是什么 + 验证/颠覆了哪个认知），但：

- 无"用户填写体证 → 状态流转"的机制
- 无"体证进入公共智慧池"的通道（SOP Step 7"可选择性上传至社区（匿名）"未落地）
- 无跨边界信任护栏

### 1.2 目标

1. **个人闭环**：用户填写"证"三问 → 卡片 `draft → completed`（用户亲证即验证，不加外部审核）
2. **公共智慧池**：completed 卡片**可选**匿名化入池 → 聚合体证洞察 → 反哺检索
3. **信任边界**：个人→公共边界设三道轻量护栏（机器合规检查 + 用户确认 + 信任标记），**不引入审核员实体**

### 1.3 非目标（奥卡姆）

- ❌ 不新增人工审核流程（无审核员角色）
- ❌ 不强制上传（上传始终可选，SOP Step 7）
- ❌ 不引入新数据库（公共智慧池 = 晶体库内新增 `wisdom/` 目录 + 索引，沿用 OKF 契约）
- ❌ 不新增共享服务/存储——仅共享一个纯常量模块（`contracts.py`，消除硬编码重复）

---

## 二、总体设计

### 2.1 数据流总览

```
① 生成（S3 已有）              ② 践行 + 填写（新增）          ③ 回流（新增，可选）
┌──────────────┐   draft   ┌────────────────────┐  completed  ┌─────────────────────┐
│ build_tizheng │ ────────► │ 用户践行 + 填"证"三问 │ ──────────► │ 公共智慧池（匿名）    │
│ _card.py      │  Tizheng  │                    │             │ wisdom/ + 索引       │
└──────────────┘  Card     └────────────────────┘             └─────────────────────┘
                                │                                  ▲
                                │ 边界①（不加审核）                 │ 边界②（三道护栏）
                                ▼                                  │
                          completed 卡片                   机器检查 + 用户确认 + 信任标记
                          （留在 tizheng/）
```

### 2.2 状态机（v1.1：三级撤回统一）

```
draft ──(填"证"三问 + 确认)──► completed ──(可选 + 匿名化 + 确认)──► published（入池）
  │  ▲                            │  ▲
  │  │                            │  │
  │  └──(撤回: completed→draft)────┘  └──(撤池: published→completed，不直接回 draft)
  │
  └──(废弃：不践行)──► retired

状态语义（对齐晶体契约 IF-2026-006）：
  draft      生成待践行
  completed  用户亲证完成（体证三问已填）
  published  匿名化入公共智慧池（仅池中副本；原卡片保持 completed）
  retired    用户放弃践行（显式标记，KSMG G3）

撤回规则（v1.1 修正）：
  completed → draft：个人闭环撤回（简单，直接改状态）
  published → completed：从池中移除副本 + 原卡片恢复 completed
  published 不回 draft：池中已有副本，直接回 draft 会留下"幽灵条目"
```

**关键点**：`published` 是池中**副本**的状态，原卡片 `status` 保持 `completed`（引用不复制——原卡片引用池条目，池条目引用原卡片，双向 ref）。

---

## 三、边界①：个人闭环（draft → completed）

### 3.1 设计决策

**不加外部审核、不加"再验证"**（用户确认采纳）。理由：

- 体证是**第一人称体验**——审核自己的体验在逻辑上不成立
- S3 定案 `verified: human:hjj`（human-reviewed 层）已是最高信任层级
- 与诊断晶体的 `process:quality-gate`（机器确认）形成双层，职责已分

### 3.2 完成条件（机器校验，非审核）

用户提交"证"三问时，生成器做**格式自检**（防空/防占位，不算审核）：

| 检查 | 规则 | 失败处理 |
|------|------|----------|
| 三问完整 | 做到了/体验/认知验证 三问均非空 | 提示补全，不流转 |
| 非占位 | 不含"（用户践行后填写"等模板残留 | 提示重写 |
| 长度下限 | 每问 ≥ 10 字（防敷衍） | 提示补充 |

自检通过 → `status: draft → completed`，frontmatter 追加：

```yaml
x-evidence:                    # 新增：体证证据块
  completed_at: 2026-08-15T21:30:00Z
  done: 我做到了什么……          # 三问原文（个人文件，不匿名）
  experience: 我的体验是什么……
  cognition_shift: 验证/颠覆了哪个认知……
  word_count: { done: 42, experience: 58, cognition_shift: 35 }
```

### 3.3 命令（个人闭环，脚本 B-1）

```bash
python complete_tizheng.py --card output/crystals/tizheng/tizheng_hjj_20260812_044309.md \
    --done "……" --experience "……" --cognition "……"
# 校验通过 → status: completed + x-evidence 写入
```

---

## 四、边界②：个人→公共（completed → 公共智慧池）

### 4.1 三道护栏（机器自动 + 用户确认，无人工审核员）

```
completed 卡片
  │
  ▼
护栏1 · 匿名化核验（机器）
  ├─ 扫描 x-evidence 与事件文本：姓名/手机/邮箱/公司/地点等 PII 模式
  ├─ 命中 → 标记需脱敏，提示用户修改后重提
  └─ 通过 → 继续
  │
  ▼
护栏2 · 完整度校验（机器）
  ├─ 三问非空 + 长度 ≥ 10（复用边界①自检）
  ├─ 含具体体验（非纯感想：experience 有动词+情境词）
  └─ 通过 → 继续
  │
  ▼
护栏3 · 用户确认（唯一人类环节，用户即审核者）
  ├─ 展示匿名化预览（脱敏后全文 + 扫描边界说明）
  ├─ 用户确认上传 → 继续
  └─ 用户取消 → 中止（不入池）
  │
  ▼
入池：生成 wisdom/<id>.md（published 副本）+ 信任标记
```

### 4.2 护栏规格明细（v1.1 增强）

| 护栏 | 检查 | 输出/失败处理 |
|------|------|---------------|
| **1 · 匿名化核验** | PII 模式扫描：姓名/手机/邮箱/公司名/地点等 | 命中 → 拦截 + 提示脱敏位置；通过 → 输出**扫描边界说明**：`已扫描：姓名/手机/邮箱/公司名；未扫描：上下文推断性识别`（审核 A2）——随匿名化预览一并展示给用户 |
| **2 · 完整度校验** | 三问非空 + ≥10 字 + experience 含动词+情境词 | 失败 → **建设性指引**："请补充具体情境（时间/地点/人物/行为）"（审核 A3），非仅拒绝；通过 → 输出 `depth` 数据（字数/has_situation）供池条目复用 |
| **3 · 用户确认** | 展示匿名化预览 + 扫描边界说明 | 用户确认 → 入池；取消 → 中止。**用户即审核者** |

### 4.3 信任标记（防"一人之言"冒充"普遍真理"）

池中每条目 frontmatter 强制携带：

```yaml
type: WisdomInsight            # 池内新类型（晶体契约扩展，见 §5）
status: published
verified: { by: human:anonymous, at: ... }   # 匿名亲证（非 process！）
x-evidence:
  sample_size: 1               # 该洞察的体证人数（防以偏概全）
  depth: { word_count: 135, has_situation: true }   # v1.1 新增（审核 A5）：复用护栏2数据
  anonymization: best-effort   # v1.1 新增（审核 A4）：明确匿名化级别
  pooled: true                 # 已匿名化入池
  source_ref: tizheng/tizheng_hjj_....md     # 回指原卡片（引用不复制）
```

**检索端语义**（对接 S4 `retrieve_crystals.py`）：
- `verified: human:anonymous` 条目在 View 层**永远标注样本量**——`（体证 1 人）`
- v1.1 增强：有 `depth` 时展示 `（体证 1 人 · 深度体证）`，区分"深度践行"与"敷衍填写"（审核 A5）
- v1.1 增强：**永远标注匿名化级别**——`匿名化：尽力而为（模式扫描）`，与"完全匿名"区分（审核 A4）

### 4.4 命令（回流，脚本 B-2）

```bash
python pool_tizheng.py --card output/crystals/tizheng/tizheng_hjj_....md \
    --confirm yes    # 唯一交互：用户确认匿名化预览（含扫描边界说明）
# 护栏全过 → output/crystals/wisdom/<id>.md（published）+ 原卡片 x-ref.pooled 回注
```

---

## 五、公共智慧池数据设计

### 5.1 目录与索引（沿用晶体库 OKF Bundle）

```
output/crystals/
├── wisdom/                     # 新增（KnowledgeDomain 预留的伴生结构）
│   ├── index.md                # 池总览（N 条体证洞察 + 样本量分布）
│   └── w-20260812-220000-a3f2b1.md   # 单条（published）
├── tizheng/                    # 原卡片（completed）
└── index.md                    # 晶体库总览（更新：+ wisdom 区）
```

**ID 生成（v1.1 修正，审核 A8）**：`w-{YYYYMMDD}-{HHMMSS}-{hash6}.md`——时间戳 + 短哈希，天然防同日碰撞，无计数器维护。

### 5.2 WisdomInsight 条目 schema（对齐 OKF）

```yaml
---
type: WisdomInsight
title: 体证洞察：放下执念后的平静（识位·低）
status: published
generated: { by: pool_tizheng.py/0.1, at: 2026-08-15T22:00:00Z }
verified: { by: human:anonymous, at: 2026-08-15T21:30:00Z }
sources:
  - id: mirror-10-识位轴-低
    resource: daojing_database_v2.json#dimensions
x-mirror:
  chapter: 10
  dimension: 识位轴
  p_level: 低
  classic: 载营魄抱一
x-wuxing:
  weak: 水
  complement: 水
x-evidence:
  sample_size: 1
  depth: { word_count: 135, has_situation: true }
  anonymization: best-effort
  pooled: true
  source_ref: tizheng/tizheng_hjj_....md
x-relation:                      # 与 S4 检索打通
  related_classics: [第10章 · 载营魄抱一]
---
正文：匿名化后的体证摘要（三问精炼版，≤200 字）
※ 摘要为引用性精炼（对原卡片三问的缩写/脱敏），非原文复制——契约"引用不复制"例外条款（审核 A9）
```

**新增实体审计（奥卡姆）**：
- `WisdomInsight` 类型——**必须**：池中副本需要区别于原卡片的类型语义（published 副本 + 匿名）
- `x-evidence` 块（含 v1.1 的 `depth`/`anonymization`）——必须：样本量/深度/匿名级别是信任护栏的载体
- `contracts.py` 共享模块——必须：消除 VALID_TYPES 5 处硬编码重复（审核 A1，G6 精神）
- 无新数据库、无新服务——沿用晶体库目录 + 现有脚本

---

## 六、B-3 回注（经典↔践行双向检索，S4 串联）

completed/published 的体证按镜鉴章回注 ClassicalInsight：

```yaml
# classical_v02/10.md frontmatter（生成时自动读取数据库回注）
x-application:
  tizheng_count: 1              # 本章关联体证数
  wisdom_refs: [wisdom/w-20260812-220000-a3f2b1.md]
```

- **持久化方案（v1.1 定案，审核 A6）**：回注**不写晶体文件**（全量重生成会丢），而是写 `daojing_database_v2.json` → 章节点新增 `x_application` 字段（v2 schema 扩展，可选向后兼容）；`build_classical_crystals_v02.py` 生成时自动读取写入晶体 frontmatter——**数据源是可信源**，重生成不丢
- 触发时机：体证入池时自动回注（B-2 内联，写数据库）
- 检索效果：S4 `--kw 载营魄抱一` → 命中经典章 + 相关体证/智慧条目（双向可达）
- **只回注池中（匿名）条目**——个人 completed 卡片不回注（隐私）

---

## 七、与既有体系对齐

| 体系 | 对齐点 |
|------|--------|
| OKF 契约 IF-2026-006 | 状态语义（draft/completed/published）、verified 双层、引用不复制（摘要例外条款已标注） |
| verified 双层 | `human:anonymous`（池）≠ `process:quality-gate`（诊断）——匿名亲证明确标记 |
| KSMG G3 | retired 显式标记；completed/published 生命周期可追溯 |
| KSMG G6 | 机器校验制度化（自检/护栏）+ **VALID_TYPES 共享契约模块**（v1.1：契约与代码同步） |
| S4 检索器 | `retrieve_crystals.py` 增加 WisdomInsight 类型 + 样本量/深度/匿名级别展示 |
| SOP Step 7 | "可选择性上传（匿名）"原样落地——上传始终可选 |

---

## 八、实施计划

### 8.1 脚本清单（2 个新脚本 + 1 个共享模块 + 3 处扩展）

| 脚本 | 内容 | 规模 |
|------|------|:----:|
| `contracts.py`（新，审核 A1） | 共享 VALID_TYPES / VALID_VERIFIED / WUXING_ORDER 等常量 | ~20 行 |
| `complete_tizheng.py`（新） | 填证三问 → 自检 → draft→completed + x-evidence | ~120 行 |
| `pool_tizheng.py`（新） | 三道护栏 → 匿名化 → wisdom 条目 + 数据库回注 | ~160 行 |
| `build_tizheng_card.py`（改） | VALID_TYPES 改引 contracts.py | ~5 行 |
| `build_classical_crystals_v02.py`（改） | VALID_TYPES 改引 contracts.py + 读取 x_application 回注 | ~20 行 |
| `export_crystals.py`（改） | VALID_TYPES 改引 contracts.py | ~5 行 |
| `retrieve_crystals.py`（改） | WisdomInsight 类型 + 样本量/深度/匿名级别展示 | ~40 行（审核：原估 20 偏乐观） |

### 8.2 里程碑

| 阶段 | 交付 | 验收 |
|------|------|------|
| M0 | contracts.py 抽取 + 5 处脚本改造 | 全部脚本引共享模块，行为不变（回归） |
| M1 | complete_tizheng.py + 自检规则 | 实测：填证 → completed；空/占位拦截 + 建设性指引 |
| M2 | pool_tizheng.py + 三道护栏 | 实测：含 PII 拦截（含扫描边界说明）；确认后入池；depth/anonymization 字段正确 |
| M3 | 数据库回注（方案 A）+ S4 扩展 | 实测：入池后 `daojing_database_v2.json` x_application 更新；**重生成晶体不丢**；S4 检索显示样本量/深度/匿名级别 |
| M4 | 交付包 + 报告 | 全链路演示（生成→填证→入池→检索） |

---

## 九、验收标准

- [ ] 边界①：填证三问后卡片 `completed`，frontmatter 含 `x-evidence`（自检拦截空/占位，给出"请补充具体情境"指引）
- [ ] 边界②：含 PII 卡片被护栏 1 拦截（输出扫描边界说明）；确认后入池，`verified: human:anonymous`
- [ ] 信任标记：池条目 `sample_size` + `depth` + `anonymization: best-effort` 正确；S4 检索显示样本量/深度/匿名级别
- [ ] 持久化：入池回注写数据库（方案 A）；**`build_classical_crystals_v02.py` 重生成后 x-application 不丢**
- [ ] 契约同步：VALID_TYPES 全部引 `contracts.py`（无硬编码残留，grep 验证）
- [ ] 引用不复制：池条目仅 ref 原卡片 + ≤200 字精炼摘要（已标注例外条款）
- [ ] 隐私：个人 completed 卡片不回注经典章；仅池中匿名条目回注
- [ ] 可复跑：全流程两次执行 diff 仅时间戳/ID

---

## 十、风险与边界

| 风险 | 缓解 |
|------|------|
| 匿名化不彻底（AI 生成内容仍可指认作者） | 护栏 1 PII 模式扫描 + 用户预览确认 + **扫描边界显式标注**（v1.1）；**不承诺绝对匿名**，池条目 `anonymization: best-effort` |
| 体证质量参差（泛泛而谈） | 护栏 2 完整度检查 + **建设性指引**（v1.1）；`depth` 字段区分深度/敷衍 |
| 一人之言被当普遍真理 | 样本量 + 深度强制展示（`sample_size: 1 · 深度体证` 不隐藏） |
| 撤回困难 | 三级撤回路径（completed→draft；published→completed，不产生幽灵条目） |
| 池条目 ID 碰撞 | 时间戳+短哈希（v1.1） |

---

## 十一、一句话定位

> **体证回流 = 两个边界 + 一个池：个人闭环（draft→completed）不加审核——用户亲证即验证（human 层）；个人→公共（completed→published）设三道轻量护栏——机器合规检查（匿名化/完整度，边界显式标注）+ 用户确认（唯一人类环节）+ 信任标记（样本量/深度/匿名级别永远可见）。不引入审核员实体，公共智慧池是晶体库的 wisdom/ 目录而非新系统。回注走数据源（方案 A）保证重生成不丢，契约常量入共享模块（contracts.py）杜绝硬编码漂移。体证从"个人修行记录"升级为"可被检索、可反哺经典、样本量透明的公共智慧"——个人地图向安人地图的通道正式开通。**

*审核记录: 2026-08-12 审核报告（有条件通过）→ 4 建议 + 3 发现 + 1 确认全部采纳 → v1.1 定稿*
