# 五行八卦推理规则库 · 独立提案 v1.0

> 将五行八卦推断逻辑从构建脚本中解耦，建立可自我演化的独立规则引擎。

---

## 一、问题陈述

### 当前困境

五行八卦属性目前有两种生成方式：

| 方式 | 优点 | 缺点 |
|:-----|:-----|:-----|
| 人工编写 JSON | 精准、有学术深度 | 不可扩展，每套数据集需人工逐一标注 |
| LLM 生成时推断 | 自动化 | 规则散落在 prompt 中，无法积累经验，每次重新生成 |

**核心矛盾**：规则是隐式的（藏在 LLM 的训练数据中），无法被检查、修正、版本化、复用。

---

## 二、设计目标

1. **规则显式化**：五行/八卦推断规则以结构化 JSON 存储，可审查、可编辑
2. **自我演化**：用户在管理面板中修正属性后，规则库自动学习（频率统计 + 置信度调整）
3. **知识领域隔离**：不同知识体系（中医/哲学/AI）有独立的规则权重，互不干扰
4. **向后兼容**：现有数据集无需任何修改，规则库为可选增强

---

## 三、架构设计

### 3.1 文件结构

```
skills/mobius-concept-map/
├── assets/
│   └── template.html          # 不变
├── scripts/
│   ├── build_mobius.py        # 不变（读取 rules.json）
│   └── wuxing_engine.py       # 新增：规则引擎
└── rules/
    ├── wuxing_bagua_rules.json # 核心规则库
    └── domains/               # 领域专属规则覆盖
        ├── chinese_medicine.json
        ├── buddhism.json
        ├── confucianism.json
        ├── daojism.json
        ├── western_philosophy.json
        └── science_technology.json
```

### 3.2 规则库 JSON 结构

```json
{
  "version": "1.0",
  "updated": "2026-07-23",
  "wuxing": {
    "木": {
      "keywords": ["生长", "展开", "创造", "起始", "生发", "学习", "动力", "发展", "迸发", "突破"],
      "en_keywords": ["growth", "create", "begin", "learn", "develop", "emerge", "initiate"],
      "concept_types": ["过程", "机制", "动态"],
      "weight": 1.0
    },
    "火": {
      "keywords": ["发散", "极致", "照明", "执着", "热情", "燃点", "明照", "觉察", "洞见", "显化"],
      "en_keywords": ["illuminate", "extreme", "attach", "passion", "insight", "manifest"],
      "concept_types": ["觉察", "极致状态"],
      "weight": 1.0
    },
    "土": {
      "keywords": ["承载", "中和", "基础", "本体", "根源", "稳定", "归中", "含藏", "孕育", "根基"],
      "en_keywords": ["foundation", "contain", "stabilize", "base", "root", "embody"],
      "concept_types": ["定义", "本质", "基础概念"],
      "weight": 1.0
    },
    "金": {
      "keywords": ["收敛", "回归", "法则", "边界", "精纯", "反思", "确定", "规则", "精炼", "断除"],
      "en_keywords": ["converge", "refine", "rule", "boundary", "reflect", "determine"],
      "concept_types": ["法则", "结果", "精炼"],
      "weight": 1.0
    },
    "水": {
      "keywords": ["流动", "贯通", "潜藏", "循环", "通道", "恒转", "潜在", "连续", "转化", "通达"],
      "en_keywords": ["flow", "connect", "latent", "cycle", "transform", "continuous"],
      "concept_types": ["流动", "转化", "潜在"],
      "weight": 1.0
    }
  },
  "bagua": {
    "☰乾": {
      "wuxing": "金",
      "virtue": "健·究竟",
      "concept_types": ["终极境界", "最高真理", "完成态", "遍照"],
      "weight": 1.0
    },
    "☱兑": {
      "wuxing": "金",
      "virtue": "悦·修习",
      "concept_types": ["悟后之喜", "平衡", "和谐", "释然", "平等"],
      "weight": 1.0
    },
    "☲离": {
      "wuxing": "火",
      "virtue": "丽·通达",
      "concept_types": ["照见", "明辨", "觉察", "洞见", "智慧"],
      "weight": 1.0
    },
    "☳震": {
      "wuxing": "木",
      "virtue": "动·加行",
      "concept_types": ["初始发动", "突破", "迸发", "觉醒", "起始"],
      "weight": 1.0
    },
    "☴巽": {
      "wuxing": "木",
      "virtue": "入·渗透",
      "concept_types": ["微细", "深入", "渗透", "法则性", "系统"],
      "weight": 1.0
    },
    "☵坎": {
      "wuxing": "水",
      "virtue": "陷·资粮",
      "concept_types": ["潜流", "积累", "深层", "隐伏", "困境中的坚持"],
      "weight": 1.0
    },
    "☶艮": {
      "wuxing": "土",
      "virtue": "止·不动",
      "concept_types": ["静止", "不动", "安住", "如如", "定境"],
      "weight": 1.0
    },
    "☷坤": {
      "wuxing": "土",
      "virtue": "顺·承载",
      "concept_types": ["含藏", "承载", "孕育", "厚德", "根源"],
      "weight": 1.0
    }
  },
  "layer_assignment": {
    "outer": {
      "label": "体·根源",
      "indicators": ["是什么", "定义", "本质", "基础", "根源", "本体", "含藏", "阿赖耶"],
      "weight": 1.0
    },
    "middle": {
      "label": "相·过程",
      "indicators": ["怎么运作", "过程", "关系", "机制", "现行", "作用", "互动"],
      "weight": 1.0
    },
    "inner": {
      "label": "用·境界",
      "indicators": ["指向哪里", "境界", "超越", "终极", "智慧", "果位", "成就"],
      "weight": 1.0
    }
  }
}
```

### 3.3 领域覆盖示例

```json
// domains/chinese_medicine.json
{
  "domain": "chinese_medicine",
  "overrides": {
    "wuxing": {
      "木": {
        "fixed_mapping": {
          "肝": "木", "胆": "木", "筋": "木", "目": "木",
          "怒": "木", "风": "木", "春季": "木", "东方": "木"
        },
        "weight": 1.2
      },
      "火": {
        "fixed_mapping": {
          "心": "火", "小肠": "火", "脉": "火", "舌": "火",
          "喜": "火", "暑": "火", "夏季": "火", "南方": "火"
        },
        "weight": 1.2
      }
    }
  }
}
```

领域覆盖的三种模式：

| 模式 | 说明 | 优先级 |
|:-----|:-----|:------:|
| `fixed_mapping` | 精确映射表（如 肝→木），命中则直接返回 | 最高 |
| `keywords` 追加 | 领域特有关键词，追加到基础关键词列表 | 中 |
| `weight` 调整 | 调整某五行的推断权重（0.5-2.0） | 最低 |

---

## 四、自我演化机制

### 4.1 学习流程

```
用户在管理面板中修改某个概念的 wuxing/bagua
    ↓
前端发送 {concept, old_wuxing, new_wuxing, domain} 到规则库
    ↓
规则库更新 local_corrections.json（用户修正记录）
    ↓
后台统计：某关键词被用户修正为某五行的频率
    ↓
当频率超过阈值（如 3 次），自动提升该关键词的权重
    ↓
定期生成 diff 报告，供人类审核后合并到主规则库
```

### 4.2 修正记录格式

```json
// local_corrections.json（按 domain 隔离）
{
  "domain": "default",
  "history": [
    {
      "concept": "Attention Mechanism",
      "old_wuxing": "火",
      "new_wuxing": "木",
      "reason": "注意力是选择和聚焦，属于木的生长方向性",
      "timestamp": "2026-07-23T10:00:00Z",
      "auto_learned": false
    }
  ],
  "keyword_stats": {
    "attention": { "木": 2, "火": 1 },
    "mechanism": { "木": 1, "金": 1 }
  }
}
```

### 4.3 演化策略

| 策略 | 触发条件 | 动作 |
|:-----|:--------|:-----|
| **即时校正** | 用户修改单个概念属性 | 记录到 local_corrections.json |
| **频率学习** | 同一关键词被修正 ≥3 次指向同一五行 | 提升该关键词权重至 1.5 |
| **领域迁移** | 新领域出现 ≥10 条修正记录 | 自动生成领域覆盖文件的草稿 |
| **人工审核** | 每季度或修正记录 ≥50 条 | 生成 diff 报告，邀请人类审核 |
| **版本快照** | 主规则库每次更新时 | 保留旧版本备份（`rules_v1.0.json`） |

---

## 五、与现有系统的集成点

### 5.1 build_mobius.py 集成

```python
# 新增参数
--auto-wuxing DOMAIN     # 启用自动五行八卦推断
--rules PATH             # 指定规则库路径（默认 rules/wuxing_bagua_rules.json）
--rules-output PATH      # 导出推断结果供审查

# 调用 wuxing_engine.py
from wuxing_engine import WuxingEngine
engine = WuxingEngine(rules_path=args.rules, domain=args.auto_wuxing)
for concept in rings:
    concept['wuxing'] = engine.infer_wuxing(concept['label'], concept.get('desc', ''))
    concept['bagua'] = engine.infer_bagua(concept['label'], concept.get('desc', ''), concept['wuxing'])
```

### 5.2 管理面板集成

在 HTML 管理面板中新增：

- 概念编辑表单增加 `wuxing` 和 `bagua` 下拉选择器
- 修改保存时，如果属性值与规则库推断结果不同，弹出"是否提交修正？"
- 新增"导出修正记录"按钮（JSON 下载）

### 5.3 template.html 集成

在 `initData()` 中增加规则推断逻辑：

```javascript
// 如果概念缺少 wuxing/bagua 且已嵌入规则库，则自动推断
if (window.WUXING_RULES && !concept.wuxing) {
    concept.wuxing = inferWuxing(concept.label, concept.desc);
    concept.bagua = inferBagua(concept.label, concept.desc, concept.wuxing);
}
```

---

## 六、规则引擎核心算法

### 6.1 五行推断（加权关键词匹配）

```
输入：concept.label + concept.desc
输出：wuxing（木/火/土/金/水）

for each wuxing in [木,火,土,金,水]:
    score = 0
    for keyword in wuxing.keywords:
        if keyword in label: score += 3 * weight    # 标题命中权重更高
        if keyword in desc:  score += 1 * weight   # 描述命中
    for type in wuxing.concept_types:
        if type matches layer_position: score += 2 * weight  // 层位加分
    wuxing.scores[wuxing] = score

return argmax(scores)  // 取最高分
```

### 6.2 八卦推断（五行 + 卦德匹配）

```
输入：concept.label + concept.desc + concept.wuxing
输出：bagua（☰乾/☱兑/...）

先按 wuxing 过滤出 2 个候选卦（如 金→乾/兑）
for each candidate:
    score = base_weight
    for concept_type in bagua.concept_types:
        if type in desc: score += 2
return argmax(scores)
```

### 6.3 质量校验

推断完成后执行三项检查：

| 检查 | 规则 | 不通过时处理 |
|:-----|:-----|:----------|
| 五行分布均衡 | 每层五行种类 ≥ 3 | 对过低分的概念重新匹配次优五行 |
| 八卦覆盖度 | 八卦覆盖 ≥ 6 / 8 | 将重复卦位替换为未覆盖卦 |
| 相生链连续 | 木→火→土→金→水 在外→中→内层基本成立 | 调整最弱环节 |

---

## 七、版本管理

| 版本 | 内容 |
|:-----|:-----|
| v1.0 | 基础规则库（本文档定义的全部规则）+ 6 个领域覆盖 |
| v1.1 | 自我演化 + 用户修正记录格式 |
| v2.0 | 多语言支持（中/英关键词）+ 跨领域融合规则 |

规则库随 `build_mobius.py` 一起发布在 GitHub `skills/mobius-concept-map/rules/` 目录下。

---

## 八、非目标

- **不做**：让规则引擎替代人工标注（规则是辅助，不是权威）
- **不做**：自动修改用户已确认的属性（只在用户请求时才推断）
- **不做**：领域间的规则自动迁移（需人工审核）
