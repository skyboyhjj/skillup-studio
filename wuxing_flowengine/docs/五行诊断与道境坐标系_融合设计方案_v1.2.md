# 五行诊断与道境坐标系：融合设计方案 V1.2

> **V1.2 更新说明**（2026.07.28）：基于评审意见深化 9.3 遗留问题——S 公式上限穷举分析（权重互斥约束导致四维最大值不可兼得）、Kᵧ 负值声明修正为 [0,1]、孔子案例 S 值手算验证（发现 200 倍偏差）、小禾案例阶段判定代码走读（发现生→克不一致）、8.2 历史轨迹示例数字验算（发现 T1~T4 全部与公式不符）、8.1 示例权重可行性标记。所有系数维持 v1.0_initial 校准值，待 Phase 3 数据校验。

---

## 目录

- [第一章 融合动机：两个框架的互补性](#第一章-融合动机两个框架的互补性)
- [第二章 架构总览：从"诊断"到"导航"](#第二章-架构总览从诊断到导航)
- [第三章 维度映射：五行 ↔ 道境四轴](#第三章-维度映射五行--道境四轴)
- [第四章 规则融合：生克化通变的形式化](#第四章-规则融合生克化通变的形式化)
- [第五章 计算引擎：道境积分算法](#第五章-计算引擎道境积分算法)
- [第六章 案例验证：三案例的融合诊断](#第六章-案例验证三案例的融合诊断)
- [第七章 配置文件设计：从五行权重到道境参数](#第七章-配置文件设计从五行权重到道境参数)
- [第八章 输出层设计：诊断报告的结构](#第八章-输出层设计诊断报告的结构)
- [第九章 实施路径](#第九章-实施路径)
- [附录 A：已知画像库](#附录-a已知画像库)

---

## 第一章 融合动机：两个框架的互补性

### 1.1 两个框架的定位

| 框架 | 定位 | 核心问题 | 现有能力 |
|------|------|----------|----------|
| **五行诊断器 v2** | 文本/概念地图的思维特质分析 | "这篇文章的思维方式偏向哪种五行？" | 七维指标体系 + 认知深度 + 可配置权重 |
| **道境坐标系 (DRF)** | 生命成长的时空导航模型 | "这个生命处于什么道境？下一步往哪里走？" | 四维坐标轴 + 生克化通变 + 存在度方程 |

### 1.2 互补性分析

五行诊断器擅长**静态快照**——告诉你一篇文章、一个概念地图当前是什么状态。但它的局限在于：
- 只描述"是什么"，不回答"怎么变"
- 只有五行的频次分布，没有时空动态
- 只适用于文本/概念地图，不适用于生命轨迹

道境坐标系擅长**动态导航**——告诉你一个生命体在时空中的位置和运动方向。但它的局限在于：
- 四维读数（Oₜ, Eᵤ, Cₖ, Kᵧ）缺乏结构化的量化方法
- 生克化通变的判断依赖人工解读
- 缺乏与文本分析工具的对接

**融合的核心价值**：用五行诊断器的计算能力，为道境坐标系提供可量化的四维读数；用道境坐标系的生命模型，为五行诊断器赋予"下一步"的导航能力。

### 1.3 融合后的能力跃迁

```
融合前：
  五行诊断器 → "这篇文章金水主导，理性驱动"
  道境坐标系 → "这个孩子时位紊乱，需要重建时空基底"

融合后：
  道境五行引擎 → "这篇文章处于'金→水'的克变阶段，
                   识位清晰度 0.72，宇位均衡度 0.58，
                   建议下一步：引火入局以化金水之寒"
```

---

## 第二章 架构总览：从"诊断"到"导航"

### 2.1 三层架构

```
┌─────────────────────────────────────────────────┐
│              第三层：道境导航层                    │
│  生克化通变阶段判定 · 存在度 S 计算 · 下一步建议    │
├─────────────────────────────────────────────────┤
│              第二层：五行诊断层                    │
│  七维指标体系 · 认知深度 · 权重配置 · 熵值计算      │
├─────────────────────────────────────────────────┤
│              第一层：数据输入层                    │
│  概念地图 JSON · 文本标注数据 · 生命轨迹日志        │
└─────────────────────────────────────────────────┘
```

### 2.2 数据流

```
输入数据（概念地图 / 生命日志）
        │
        ▼
  [第一层] 解析 → 提取概念 + 深层标注
        │
        ▼
  [第二层] 五行诊断器 v2 → 七维输出
        │  · 五行频次分布
        │  · 层×五行矩阵
        │  · 重心偏移路径
        │  · 五行熵
        │  · 重心向量
        │  · 认知深度画像
        │  · 风格判语
        │
        ▼
  [第三层] 道境映射引擎
        │  · 五行→四维映射（Oₜ, Eᵤ, Cₖ, Kᵧ）
        │  · 生克化通变阶段判定
        │  · 存在度 S 积分计算
        │  · 临界阈值检测
        │  · 导航建议生成
        │
        ▼
  输出：道境五行诊断报告
```

---

## 第三章 维度映射：五行 ↔ 道境四轴

### 3.1 映射逻辑

五行与道境四轴不是一一对应，而是**加权组合**。每个道境维度由五行中的特定行主导，辅以其他行的调节。

### 3.2 映射矩阵

| 道境轴 | 数学定义 | 主导五行 | 辅助五行 | 映射逻辑 |
|--------|----------|----------|----------|----------|
| **时位 Oₜ** | 有序度 / 节律稳定性 | 土（承载） | 金（收敛） | 土的"承载"提供时间基底，金的"收敛"防止熵增 |
| **宇位 Eᵤ** | 能量分布均衡度 | 木（生长） | 水（流动） | 木的"生长"推动能量拓展，水的"流动"确保分布不壅塞 |
| **识位 Cₖ** | 觉知清晰度 | 水（探索） | 火（照亮） | 水的"深层涌现"是觉知的内容，火的"照亮"是觉知的形式 |
| **缘位 Kᵧ** | 因果纠缠度 | 火（传播） | 土（承载） | 火的"传播"建立关系，土的"承载"让关系稳定持续 |

### 3.3 计算公式（v1.0 初始校准值，待 Phase 3 数据校验）

> ⚠️ 所有系数标注 `_calibration: "v1.0_initial"`，将在 Phase 3 用实际数据校准。

设五行频次归一化向量为 `W = [w_木, w_火, w_土, w_金, w_水]`（各值 ∈ [0, 1]），熵值为 `H`，最大熵 `H_max = log₂(5) ≈ 2.322`，重心向量为 `(cx, cy)`，路径中相克边数量为 `ke_edge_count`（取值 0~2）：

```
Oₜ = w_土 × 0.6 + w_金 × 0.3 + (1 − H/H_max) × 0.1    [calibration: v1.0_initial]
Eᵤ = 1 − 0.5 × |w_木 − 0.25| − 0.5 × |w_水 − 0.25| − 0.3 × √(cx² + cy²)    [calibration: v1.0_initial]
Cₖ = w_水 × 0.5 + w_火 × 0.3 + w_木 × 0.2    [calibration: v1.0_initial]
Kᵧ = w_火 × 0.4 + w_土 × 0.3 + ke_edge_count / 2 × 0.3    [calibration: v1.0_initial]
```

**Eᵤ 公式修正说明**（V1.1）：原公式 `1 − |w_木−0.25| − |w_水−0.25| − |cx| − |cy|` 理论取值范围为 `[-4.5, 1]`，极端案例下会出负数。修正为引入系数（0.5/0.5/0.3）+ 用重心模长 `√(cx²+cy²)` 替代两个独立绝对值，确保 Eᵤ ∈ [0, 1]。

**Kᵧ 公式说明**（V1.1）：`ke_edge_count` 定义明确为"路径中相克边的数量"，三层路径最多 2 条边（种子→现行、现行→超越），用 `/2` 而非 `/3` 归一化。**当前公式 Kᵧ ∈ [0, 1]**（三项均为非负）。恶性因果循环的量化（负值）需在公式中引入惩罚项（如"连续被克次数"或"负向因果链计数"），Phase 3 将重新设计此项。

**Oₜ 熵项说明**（V1.1）：`1−H/H_max` 隐含"低熵=有序"假设。在概念地图上下文中，高熵=多元广博，低熵=单一聚焦——两者都是"有序"的不同形式。当前公式默认低熵场景下 Oₜ 更高，适用于分析"是否聚焦"的场景。如需分析"是否广博"的场景，可改为 `H/H_max × 0.1`。

### 3.4 存在度 S 的计算

```
S = Oₜ × Eᵤ × Cₖ × Kᵧ × 100

（乘性而非加性：四维中任一维过低，S 都会被拉低——
这正是"木桶效应"的道境表述）
```

> ⚠️ **跨领域 S 值比较限制**（V1.1）：S 的绝对值在不同领域（经典文本 vs 儿童成长 vs 管理实践）天然不可比。Phase 3 将建立 `domain_calibration` 基准。当前 S 值仅适用于同一领域内部的纵向比较。

---

## 第四章 规则融合：生克化通变的形式化

### 4.1 诊断模式：静态 vs 动态（V1.1 新增）

道境五行引擎支持两种诊断模式，由配置项 `diagnosis_mode` 控制：

| 模式 | 输入 | 可用判定 | 适用场景 |
|------|------|----------|----------|
| **static**（静态） | 单次概念地图快照 | 生、克（基于 S 的化、通、变需建立基准后可用） | 单篇文章分析、概念地图诊断 |
| **dynamic**（动态） | 多次快照的时间序列 | 全部五阶段（含 ΔH 陡降、重心位移、深度突变） | 生命轨迹追踪、多次观测对比 |

### 4.2 五阶段的形式化判定

基于五行诊断器的七维输出，判定当前处于哪个阶段。以下表格按 `diagnosis_mode` 区分：

#### 4.2.1 静态模式判定（static，单次快照）

| 阶段 | 判定条件 | 条件细化 | 依赖 |
|------|----------|----------|------|
| **生** | 任一五行 > 30% 且 H/H_max < 0.50 | 单极主导 + 聚焦 | 无 |
| **克** | `ke_edge_count ≥ 1`（路径中至少一条相克边） | 层间冲突 | 无 |
| **化** | `S > θ_critical` | 临界跃迁（需建立领域基准） | S 值基准 |
| **通** | 路径匹配已知画像 且 0.50 < H/H_max < 0.85 | 找到节奏 | 画像库 |
| **变** | 不可用（需要时间序列检测深度突变） | — | 时间序列 |

> ⚠️ **静态模式限制**：化阶段的 `S > θ_critical` 依赖 S 绝对值，需建立领域基准校准后方可信任。通阶段的画像匹配依赖附录 A 画像库。变阶段在静态模式下不可用。

#### 4.2.2 动态模式判定（dynamic，多次快照）

| 阶段 | 判定条件 | 条件细化 | 依赖 |
|------|----------|----------|------|
| **生** | 任一五行 > 30% 且 H/H_max < 0.50 | 单极主导 + 聚焦 | 无 |
| **克** | `ke_edge_count ≥ 1` | 层间冲突 | 无 |
| **化** | ΔH < -0.3（熵陡降）且 重心位移 > 0.3 | 四维同时跃迁 | 前一快照 |
| **通** | 路径匹配已知画像 且 0.50 < H/H_max < 0.85 | 找到节奏 | 画像库 |
| **变** | 认知深度主导层位移 ≥ 1 级（如 L2→L4） | 深度范式转换 | 前一快照 |

> **重心位移**定义：`√((cx₁−cx₂)² + (cy₁−cy₂)²) > 0.3`（两次快照间重心向量的欧几里得距离）。

#### 4.2.3 五行特征与道境特征

| 阶段 | 五行特征 | 道境特征 |
|------|----------|----------|
| **生** | 单极主导，积累期 | Oₜ 或 Eᵤ 单向上升 |
| **克** | 层间相克边 ≥ 1 | Oₜ 与 Eᵤ 出现互相制约 |
| **化** | 五行分布从分散→聚焦 / S 跨越临界 | 四维同时跃迁 |
| **通** | 五行呈现"通道"模式 | 时空直觉从源域迁移到目标域 |
| **变** | 深度画像从 L2 主导→L3/L4 主导 | 坐标系本身发生范式升维 |

### 4.3 五阶段与七维诊断的对应关系

```
维度一（频次分布）→ 生：是否有主导行？克：是否有对峙行？
维度二（层×五行矩阵）→ 克：层间主导行是否相克？通：层间是否有贯通模式？
维度三（重心偏移路径）→ 通：路径是否匹配已知画像？化：路径是否突变？
维度四（五行熵）→ 生：低熵=积累期；化：熵的突变；变：熵的范式转换
维度五（重心向量）→ 克：重心偏移方向；化：重心突变
维度六（特质画像）→ 生：特质雏形；通：特质贯通
维度七（风格判语）→ 化：判语修正；变：判语更新
维度八（认知深度）→ 变：深度范式转换的关键指标
```

### 4.4 生克化通变的嵌套规则

基于三案例的关键洞见——**生克化通变不是一次性完成的直线，而是每个阶段内部都嵌套着五阶段的螺旋**：

```
全局：生 → 克 → 化 → 通 → 变
        │    │    │    │    │
局部： 生克 生克 生克 生克 生克
       化通 化通 化通 化通 化通
       变   变   变   变   变
```

**形式化**：对每个阶段，计算其内部五阶段的子状态。例如：

- 在全局"克"阶段，内部可能有"生的萌芽"（宇位重建）→"克的启动"（时位约束）→"化的微光"（局部突破）→"通的尝试"（跨域迁移）→"变的准备"（范式预兆）

### 4.5 临界阈值 θ_critical 的动态计算

```
θ_critical = θ_base × f(认知深度) × g(案例类型)

θ_base = 60（基准临界值）
f(认知深度) = 1.0 (L1主导) / 1.5 (L2主导) / 2.0 (L3主导) / 3.0 (L4主导)
g(案例类型) = 1.0 (顺境·全域跃迁) / 0.6 (逆境·成核生长)
```

- 认知深度越深，临界阈值越高——"化"需要更深的积累
- 逆境案例的临界阈值更低——"成核生长"比"全域跃迁"更容易触发，但突破幅度更小

---

## 第五章 计算引擎：道境积分算法

### 5.1 算法流程图

```
输入：概念地图 JSON / 生命轨迹日志
        │
        ▼
  [步骤1] 五行诊断器 v2 运行
        │  · 七维指标体系输出
        │  · 认知深度画像
        │
        ▼
  [步骤2] 五行→四维映射
        │  · Oₜ = f(w_土, w_金, H)
        │  · Eᵤ = f(w_木, w_水, cx, cy)
        │  · Cₖ = f(w_水, w_火, w_木)
        │  · Kᵧ = f(w_火, w_土, 生克边数)
        │
        ▼
  [步骤3] 存在度 S 计算
        │  · S = Oₜ × Eᵤ × Cₖ × Kᵧ × 100
        │  · 记录历史 S 值（用于检测跃迁）
        │
        ▼
  [步骤4] 阶段判定
        │  · 五阶段判定（生/克/化/通/变）
        │  · 内部嵌套状态判定
        │  · 临界阈值比较
        │
        ▼
  [步骤5] 导航建议生成
        │  · 基于当前阶段 + 五行偏态
        │  · 引用经典文本（道德经/论语）
        │  · 输出下一步具体行动建议
        │
        ▼
  输出：道境五行诊断报告
```

### 5.2 算法伪代码

```python
import math

def diagnose_dao_realm(concept_map, config=None):
    """
    道境五行融合诊断。
    config.diagnosis_mode: "static" | "dynamic"（默认 "static"）
    """
    # 步骤1: 五行诊断
    wuxing_result = wuxing_diagnose_v2(concept_map, config)

    # 步骤2: 五行→四维映射（v1.0_initial 校准系数）
    W = wuxing_result["dim1_freq"]  # 五行频次
    H = wuxing_result["dim4_entropy"]["H"]
    H_max = wuxing_result["dim4_entropy"]["H_max"]
    cx = wuxing_result["dim5_compass"]["cx"]
    cy = wuxing_result["dim5_compass"]["cy"]
    path_edges = wuxing_result["dim3_edges"]
    depth_profile = wuxing_result.get("dim8_depth_profile", {})

    # 提取路径中相克边的数量（ke_edge_count，最大为2）
    ke_edge_count = sum(1 for e in path_edges if e["relation"] == "相克")

    O_t = W["土"]["pct"]/100 * 0.6 + W["金"]["pct"]/100 * 0.3 + (1 - H/H_max) * 0.1
    E_u = 1 - 0.5 * abs(W["木"]["pct"]/100 - 0.25) - 0.5 * abs(W["水"]["pct"]/100 - 0.25) \
          - 0.3 * math.sqrt(cx*cx + cy*cy)
    C_k = W["水"]["pct"]/100 * 0.5 + W["火"]["pct"]/100 * 0.3 + W["木"]["pct"]/100 * 0.2
    K_y = W["火"]["pct"]/100 * 0.4 + W["土"]["pct"]/100 * 0.3 + ke_edge_count / 2 * 0.3

    # 步骤3: 存在度计算
    S = O_t * E_u * C_k * K_y * 100

    # 步骤4: 阶段判定（根据 diagnosis_mode 分支）
    mode = config.get("diagnosis_mode", "static") if config else "static"
    stage = determine_stage(wuxing_result, S, config, mode)

    # 步骤5: 导航建议
    guidance = generate_guidance(stage, W, O_t, E_u, C_k, K_y)

    return {
        "wuxing_diagnosis": wuxing_result,
        "dao_realm_readings": {
            "O_t": round(O_t, 3), "E_u": round(E_u, 3),
            "C_k": round(C_k, 3), "K_y": round(K_y, 3),
            "S": round(S, 1)
        },
        "stage": stage,
        "guidance": guidance
    }
```

### 5.3 阶段判定函数

```python
def determine_stage(wuxing_result, S, config, mode="static"):
    """
    判定生克化通变阶段。
    mode="static": 单次快照，依赖 S 绝对值 + 画像库
    mode="dynamic": 多次快照，依赖 ΔH + 重心位移 + 深度突变
    """
    W = wuxing_result["dim1_freq"]
    H = wuxing_result["dim4_entropy"]["H"]
    H_max = wuxing_result["dim4_entropy"]["H_max"]
    path_edges = wuxing_result["dim3_edges"]
    path_profile = wuxing_result["dim3_profile"]
    depth_profile = wuxing_result.get("dim8_depth_profile", {})

    theta = compute_theta_critical(depth_profile, config)

    # 生：单极主导 + 低熵
    if any(W[wx]["pct"] > 30 for wx in WX_ORDER) and H/H_max < 0.50:
        return "生", {"reason": "单极主导·积累期"}

    # 克：路径中有相克边
    ke_edge_count = sum(1 for e in path_edges if e["relation"] == "相克")
    if ke_edge_count >= 1:
        return "克", {"reason": f"路径含{ke_edge_count}条相克边"}

    if mode == "dynamic":
        # 动态模式：使用历史数据判定化/变
        prev = config.get("_previous_snapshot", {})
        if prev:
            # 化：熵陡降 + 重心突变
            delta_H = prev.get("H", H) - H  # 正=熵下降
            prev_cx = prev.get("cx", 0)
            prev_cy = prev.get("cy", 0)
            centroid_disp = math.sqrt((prev_cx - cx)**2 + (prev_cy - cy)**2)
            if delta_H > 0.3 and centroid_disp > 0.3:
                return "化", {"reason": "四维同时跃迁", "delta_H": delta_H, "centroid_disp": centroid_disp}

            # 变：深度范式转换
            prev_depth = prev.get("dominant_depth", "")
            curr_depth = dominant_depth(depth_profile)
            depth_shift = depth_level_index(curr_depth) - depth_level_index(prev_depth)
            if depth_shift >= 1:
                return "变", {"reason": f"深度范式转换: {prev_depth}→{curr_depth}"}

        # 通：路径匹配画像 + 熵适中
        if path_profile and 0.50 < H/H_max < 0.85:
            return "通", {"reason": "路径匹配画像·时空直觉迁移"}

    else:
        # 静态模式：使用 S 绝对值判定化
        if S > theta:
            return "化", {"reason": f"S={S} 跨越 θ_critical={theta}"}

        # 通：路径匹配画像 + 熵适中
        if path_profile and 0.50 < H/H_max < 0.85:
            return "通", {"reason": "路径匹配画像·时空直觉迁移"}

    # 默认：回归积累
    return "生", {"reason": "未满足其他阶段条件，回归积累"}
```

---

## 第六章 案例验证：三案例的融合诊断

### 6.1 小禾案例（概念地图模拟）

以小禾12岁天文顿悟前的学习状态构建概念地图：

**输入特征**（模拟）：
- 概念分布：木 25%, 火 15%, 土 30%, 金 15%, 水 15%
- 熵值：H = 2.15（中等多元，H/H_max = 0.93）
- 重心向量：cx = -0.15, cy = 0.05（偏东·木）
- 路径：种子层（土）→ 核心层（木）→ 超越层（水）
- 深度：L2 主导

**融合诊断输出**（V1.1 公式重算，数值待 Phase 3 校准）：

```
道境四维读数（v1.0_initial 系数）：
  Oₜ = 0.23（时位有序，加深积累中）
  Eᵤ = 0.90（宇位高度均衡，书桌+天文角的空间配置合理）
  Cₖ = 0.17（识位尚浅，仍在积累期——水的深层涌现尚未激活）
  Kᵧ = 0.15（缘位萌芽，路径无相克边，因果链尚未充分展开）

存在度 S = 0.23 × 0.90 × 0.17 × 0.15 × 100 ≈ 0.5
θ_critical = 60 × 1.5(L2) × 1.0(顺境) = 90

当前阶段：生（静态模式）
  内部嵌套：生(土)→克的萌芽(木克土)→化的微光(木→水，相生)
  距临界阈值：S ≈ 0.5，需长期积累才能接近 θ_critical = 90
  预计：三个月持续积累后，S 将逐步上升

导航建议：
  "有之以为利，无之以为用"
  → 维持宇位有序（书桌+天文角），增加时位留白（无计划观察时间）
  → 识位提升路径：从"看星星"到"画星图"到"讲星图"
  → 缘位培育：邀请同学一起观测，建立"天文小组"微因果链
```

### 6.2 小石案例（概念地图模拟）

以小石9岁ADHD诊断后的状态构建概念地图：

**输入特征**（模拟）：
- 概念分布：木 10%, 火 5%, 土 15%, 金 20%, 水 50%
- 熵值：H = 1.85（中等，H/H_max = 0.80）
- 重心向量：cx = 0.25, cy = 0.40（偏西北·金水）
- 路径：金→水→水（全程无木火）
- 深度：L1 主导（认知停留在基本义）

**融合诊断输出**（V1.1 公式重算，数值待 Phase 3 校准）：

```
道境四维读数（v1.0_initial 系数）：
  Oₜ = 0.17（时位极度紊乱，注意力无法持续）
  Eᵤ = 0.66（宇位失衡，水行过高拉低均衡度）
  Cₖ = 0.29（识位：水行提供探索深度，但火木双低导致觉知偏冷）
  Kᵧ = 0.07（缘位极弱，火行仅5%——传播与关系建立能力严重不足，路径无相克边）

存在度 S = 0.17 × 0.66 × 0.29 × 0.07 × 100 ≈ 0.2
θ_critical = 60 × 1.0(L1) × 0.6(逆境) = 36

当前阶段：生（静态模式——Kᵧ 极低但非负值，S 远低于临界）
  内部嵌套：无生（时空基底破碎）→ 克的萌芽（外部干预→抗拒）→ 化的微光（恐龙域）
  距临界阈值：S ≈ 0.2，恐龙子空间 S_dinosaur 独立计算中...

导航建议：
  "图难于其易，为大于其细"
  → 不从"补课"开始，从"恐龙画册5分钟"开始
  → 宇位重建：书桌只放一本恐龙画册（空间从"任务监狱"变"兴趣入口"）
  → 时位重建：每天晚饭后翻5分钟（不需"定时间表"，只需"兴趣微光"）
  → 缘位逆转：AI学伴接管部分辅导，妈妈的焦虑因"有人分担"而自然下降
  → 识位守护：不评判"差"，只记录"恐龙域专注时长"
  → 目标：3个月内恐龙子空间 S_dinosaur 接近 θ_critical
```

### 6.3 孔子案例（宏观轨迹模拟）

孔子六阶段的宏观轨迹，每阶段以概念地图快照模拟：

| 阶段 | 年龄 | 模拟五行 | 道境阶段 | S 值 | 关键特征 |
|------|------|----------|----------|------|----------|
| 志于学 | ~15 | 木 30, 火 15, 土 20, 金 15, 水 20 | 生 | 12 | 识位轴主控，根·自爱 |
| 立 | ~30 | 木 20, 火 15, 土 30, 金 25, 水 10 | 克→化 | 48 | 时位轴主控，放弃官职，确立坐标 |
| 不惑 | ~40 | 木 15, 火 20, 土 25, 金 20, 水 20 | 通 | 72 | 缘位轴主控，拒绝阳货 |
| 知天命 | ~50 | 木 20, 火 25, 土 20, 金 15, 水 20 | 变 | 85 | 宇位轴主控，出仕与周游 |
| 耳顺 | ~60 | 木 20, 火 20, 土 20, 金 20, 水 20 | 变→新·生 | 92 | 四维趋衡 |
| 从心所欲 | ~70 | 木 20, 火 20, 土 20, 金 20, 水 20 | 坐标即道标 | 98 | 不勉而中，自然合道 |

**关键洞察**：孔子的五行分布从早期"木主导"（好奇、生长）→中期"土木金均衡"（立与不惑）→晚期"五行均衡"（从心所欲），完美对应了生克化通变→坐标系升维的完整轨迹。

---

## 第七章 配置文件设计：从五行权重到道境参数

### 7.1 扩展配置文件结构

在现有 `config_*.json` 基础上，增加道境参数段：

```json
{
  "_description": "道境五行融合配置 - 学术考据视角",
  "_version": "2.0",
  "_calibration": "v1.0_initial",

  "diagnosis_mode": "static",

  "wuxing_weights": {
    "depth_weights": {"L1": 0.5, "L2": 1.0, "L3": 2.0, "L4": 4.0},
    "layer_weights": {"种子层": 1.0, "核心层": 1.0, "超越层": 1.0},
    "wuxing_bias": {"木": 0.8, "火": 0.5, "土": 1.0, "金": 1.5, "水": 1.5}
  },

  "dao_realm_params": {
    "_calibration": "v1.0_initial",
    "mapping_coefficients": {
      "_calibration": "v1.0_initial",
      "O_t": {"土": 0.6, "金": 0.3, "entropy": 0.1},
      "E_u": {"木_deviation_coef": 0.5, "水_deviation_coef": 0.5, "centroid_modulus_coef": 0.3},
      "C_k": {"水": 0.5, "火": 0.3, "木": 0.2},
      "K_y": {"火": 0.4, "土": 0.3, "ke_edge_ratio": 0.3}
    },
    "theta_base": 60,
    "theta_depth_multipliers": {"L1": 1.0, "L2": 1.5, "L3": 2.0, "L4": 3.0},
    "theta_case_multipliers": {"global_leap": 1.0, "nucleation_growth": 0.6},
    "stage_thresholds": {
      "sheng_max_entropy_ratio": 0.50,
      "ke_edge_count_min": 1,
      "hua_static_min_S_ratio": "theta_critical",
      "hua_dynamic_entropy_drop": 0.30,
      "hua_dynamic_centroid_displacement": 0.30,
      "tong_entropy_range": [0.50, 0.85],
      "bian_depth_shift_min": 1
    }
  },

  "entropy_thresholds": {"high": 0.80, "low": 0.45},
  "dominance_threshold": 25.0,
  "scarcity_threshold": 8.0
}
```

### 7.2 三种预设视角的完整配置

| 配置项 | 学术视角 (scholarly) | 实践视角 (practical) | 教育视角 (educational) |
|--------|---------------------|---------------------|----------------------|
| **五行偏置** | 金 1.5, 水 1.5 | 木 1.5, 火 1.5 | 土 1.5, 木 1.3 |
| **深度权重** | L4:4.0, L3:2.0 | L3:4.0, L4:1.0 | L2:3.0, L3:2.0 |
| **θ_base** | 75（高门槛） | 50（低门槛） | 60（标准） |
| **案例类型** | global_leap | global_leap | nucleation_growth |
| **适用场景** | 经典文本研究 | 管理实践分析 | 儿童成长评估 |

---

## 第八章 输出层设计：诊断报告的结构

### 8.1 报告层次

```
道境五行诊断报告
├── 一、五行总体诊断（继承 v2 七维输出）
│   ├── 频次分布 + 柱状图
│   ├── 层×五行交叉矩阵
│   ├── 重心偏移路径 + 生克边
│   ├── 五行熵 + 多样性判语
│   ├── 重心向量 + 方位判语
│   ├── 五行特质画像
│   ├── 一句话风格判语
│   └── 认知深度画像
│
├── 二、道境四维读数（新增）
│   ├── 时位 Oₜ = 0.62（节律·有序）
│   ├── 宇位 Eᵤ = 0.58（能量·均衡）
│   ├── 识位 Cₖ = 0.45（觉知·积累中）
│   ├── 缘位 Kᵧ = 0.40（因果·萌芽）
│   └── 存在度 S = 6.5（距临界 83.5）
│
├── 三、生克化通变阶段判定（新增）
│   ├── 当前阶段：生（积累期）
│   ├── 内部嵌套：生(土)→克(木克土)→化萌芽
│   ├── 临界阈值：θ = 90
│   └── 历史轨迹（如有多次快照）
│
├── 四、导航建议（新增）
│   ├── 经文引用 + 道境解读
│   ├── 四维提升路径
│   ├── 风险预警（熵增、负向螺旋）
│   └── 下一步具体行动
│
└── 五、附录：原始数据表
```

### 8.2 历史轨迹输出（多次快照对比）

```
时间轴上的道境变化：
  T1: S= 3.2  ⟩ 生  ⟩  ○ ○ ○ ○  (Oₜ=0.2, Eᵤ=0.3, Cₖ=0.1, Kᵧ=0.0)
  T2: S= 6.5  ⟩ 生  ⟩  ● ○ ○ ○  (Oₜ=0.6, Eᵤ=0.5, Cₖ=0.4, Kᵧ=0.4)
  T3: S=18.0  ⟩ 克  ⟩  ● ● ○ ○  (Oₜ=0.7, Eᵤ=0.6, Cₖ=0.5, Kᵧ=0.5)
  T4: S=89.0  ⟩ 化  ⟩  ● ● ● ●  (Oₜ=0.9, Eᵤ=0.8, Cₖ=0.8, Kᵧ=0.7) ⚡临界！
```

---

## 第九章 实施路径

### 9.1 实施阶段

| 阶段 | 内容 | 产出 | 优先级 |
|------|------|------|--------|
| **Phase 1** | 扩展 `wuxing_diagnose_v2.py`，增加 `diagnose_dao_realm()` 函数 | `wuxing_diagnose_v3.py` | P0 |
| **Phase 2** | 扩展配置文件格式，增加 `dao_realm_params` 段 | 新版 `config_*.json` | P0 |
| **Phase 3** | 用三案例（小禾/小石/孔子）验证映射公式和阈值 | 验证报告 + 参数调优 | P0 |
| **Phase 4** | 增加历史轨迹对比功能（多次快照的道境变化） | `--history` 模式 | P1 |
| **Phase 5** | 导航建议生成器（基于规则 + 经典文本库） | 建议引擎 | P1 |
| **Phase 6** | 可视化输出（四维雷达图 + 轨迹图） | 可视化模块 | P2 |

### 9.2 V1.1 已解决的决策点

以下问题已在 V1.1 中修正，不再需要决策：

1. **Eᵤ 公式负值** → 已修正为系数+模长方案，Eᵤ ∈ [0, 1]
2. **化判定不一致** → 已区分静态/动态双模式
3. **时间序列依赖** → 已增加 `diagnosis_mode` 配置
4. **生克边数定义不统一** → 已统一为 `ke_edge_count` = 路径中相克边数量
5. **系数标注** → 所有系数已标注 `_calibration: "v1.0_initial"`
6. **Kᵧ 取值范围** → 已修正为 [0, 1]（三项均为非负），负值需 Phase 3 引入惩罚项后重新设计

### 9.3 Phase 3 验证时需确认的遗留问题

#### P0 级（阻塞 Phase 3 验证）

**1. S 理论上限远低于 θ_critical（🔴 公式结构问题）**

经穷举验证：五行权重互斥约束下，S 的理论上限仅 **1.47**（Oₜ=0.335, Eᵤ=0.875, Cₖ=0.200, Kᵧ=0.250），而当前 θ_critical = 36~90。

**为什么这不是调参能解决的？** 关键在于四维各自的"最大值"无法同时达到。Oₜ 要最大需要 w_土 拉满，Cₖ 要最大需要 w_水 拉满——但五行权重之和为 1，w_土 和 w_水 不能同时接近 1。在穷举搜索中，各维度最大值分别出现在完全不同的权重配置下：

| 维度 | 理论最大值 | 需要的权重配置 | 与其他维度的冲突 |
|------|-----------|---------------|-----------------|
| Oₜ | ~0.70 | w_土=1, H=0 | 与 Cₖ 冲突（w_水 需接近 1） |
| Eᵤ | ~0.95 | w_木≈0.25, w_水≈0.25, cx=cy=0 | 与 Oₜ 部分冲突（w_土 需接近 1） |
| Cₖ | ~0.50 | w_水=1 | 与 Oₜ 严重冲突（w_土 需接近 1） |
| Kᵧ | ~0.55 | w_火=1, ke_edge_count=2 | 与 Oₜ/Cₖ 均冲突 |

因此四维乘积的**真实上限远低于各维度最大值直接相乘**。即使穷举所有可能的权重组合，S 的最高值也仅 1.47——这意味着不是"当前系数不够好，需要校准"，而是**在乘性公式结构下，θ_critical 在数学上根本不可达**。

**Phase 3 建议**：在收集数据前，先做公式结构可行性论证——穷举所有可能的权重组合，确认 S 的理论上限；然后决定是（A）改乘性为加权乘性/加性混合、（B）大幅降低 θ_base 至 S 上限附近、（C）引入缩放因子，还是（D）重新设计 S 的公式结构。选型后再收集数据做参数校准。

**2. Kᵧ 负值声明与公式矛盾（🔴 文档与实现不一致）**

3.3 节原声称 Kᵧ ∈ [-1, 1]，但公式三项均为非负，实际范围为 [0, 1]。恶性因果循环的量化（负值）需在 Phase 3 中引入惩罚项（如"连续被克次数"或"负向因果链计数"）后重新设计。当前修正为 Kᵧ ∈ [0, 1]。

**3. 孔子案例 S 值与公式不对应（🔴 案例数据失真）**

6.3 表格中孔子六阶段的 S 值（12/48/72/85/92/98）与公式计算值相差约 200 倍。**这些数字是叙事性占位，未经过公式验证**。

以"从心所欲"阶段为例，代入公式手算验证：

| 步骤 | 计算 | 结果 |
|------|------|------|
| H（五行均匀分布） | H = log₂(5) = H_max | 2.322 |
| 熵项 | 1−H/H_max = 0 | 0 |
| Oₜ | 0.2×0.6 + 0.2×0.3 + 0×0.1 | 0.18 |
| Eᵤ | 1−0.5×\|0.2−0.25\|−0.5×\|0.2−0.25\|−0.3×√(0²+0²) | 0.95 |
| Cₖ | 0.2×0.5 + 0.2×0.3 + 0.2×0.2 | 0.20 |
| Kᵧ（假设 ke=0） | 0.2×0.4 + 0.2×0.3 + 0 | 0.14 |
| **S** | 0.18 × 0.95 × 0.20 × 0.14 × 100 | **≈ 0.48** |

表格写的是 **S = 98**，两者相差约 200 倍。反过来核对 6.1 小禾案例，Oₜ、S 的手算结果与文档给出的数字是吻合的——说明作者认真跑过 6.1 的公式，但 6.3 孔子案例的 S 值更像是"根据故事情节手写的示意性数字"，并未代入公式验证。既然第六章标题叫"案例验证"，数字与公式对不上会让"验证"的说法站不住脚。

Phase 3 需用真实公式重新计算全部案例数据，"案例验证"一章才能名副其实。

#### P1 级

**4. 小禾案例阶段判定与代码逻辑不对齐**

6.1 结论写"当前阶段：生"，但按 5.3 节 `determine_stage()` 代码逻辑逐行走读：

```
输入：小禾案例
  W = {木:25%, 火:15%, 土:30%, 金:15%, 水:15%}
  H/H_max = 0.93
  path_edges = [{from:"土", to:"木", relation:"相克"}]
  → ke_edge_count = 1

判定顺序：
  1. 生条件：any(W[wx].pct > 30) → 土=30%，不满足">30%"（严格大于）
     → 生条件不触发，跳过

  2. 克条件：ke_edge_count >= 1 → 1 >= 1 ✅
     → 应返回 "克"，reason: "路径含1条相克边"

文档结论：生 → 代码应判：克 → 不一致
```

6.1 文字描述将"木克土"仅当作"生阶段内部的克的萌芽"——这是一个合理的嵌套解释，但与 `determine_stage()` 的判定顺序矛盾：代码先判生、再判克，生不满足时克会直接接管全局阶段判定。需在 Phase 3 中决定：（A）调高生阈值使土=30% 也能触发"生"、（B）修改判定顺序让嵌套状态优先于全局判定、（C）保持代码逻辑并修正文档描述。Phase 3 需用真实代码跑一遍三案例，确保文字描述与代码逻辑完全对齐。

**5. 映射系数校准**

所有 v1.0_initial 系数需要用实际数据（道德经 81 章 + 三案例）校准。

**6. 画像库覆盖度**

附录 A 的 7 个画像是否覆盖足够的路径模式，Phase 3 需扩展至 12+。

**7. 跨领域 S 值比较**

需建立 `domain_calibration` 基准。

**8. 8.2 历史轨迹示例数字与公式不对应（🟡 示例数据失真）**

8.2 的四行轨迹示例中，四维读数和 S 值与乘性公式对不上。逐行验算：

| 快照 | Oₜ×Eᵤ×Cₖ×Kᵧ×100 | 实际 S | 表格 S | 问题 |
|------|-------------------|--------|--------|------|
| T1 | 0.2×0.3×0.1×0.0×100 | **0** | 3.2 | Kᵧ=0.0 时乘性必得 0 |
| T2 | 0.6×0.5×0.4×0.4×100 | **4.8** | 6.5 | 偏差 35% |
| T3 | 0.7×0.6×0.5×0.5×100 | **10.5** | 18.0 | 偏差 71% |
| T4 | 0.9×0.8×0.8×0.7×100 | **40.3** | 89.0 | 偏差 120% |

T1 最致命——Kᵧ=0.0 导致乘性结果必为 0，但表格写 3.2，说明这些数字从未用公式验算过。此外 T4 的 Oₜ=0.9 和 Cₖ=0.8 分别超出穷举的理论上限（≈0.70 和 ≈0.50），在当前公式下不可能出现。这与孔子案例（P0 第 3 条）是同一类问题——"报告输出模板"里放着算不出来的例子，后续开发照着抄示例格式时容易被带偏。Phase 3 需用真实公式重新生成 8.2 的全部示例数据。

#### P2 级

**9. 8.1 示例数字的权重可行性待验证**

8.1 的"道境四维读数"示例（Oₜ=0.62, Eᵤ=0.58, Cₖ=0.45, Kᵧ=0.40, S=6.5）乘法验证通过（0.62×0.58×0.45×0.40×100≈6.5），但结合 P0 第 1 条的冲突分析：Oₜ=0.62 需要 w_土 较高，Cₖ=0.45 已接近其 0.50 的理论上限、需要 w_水 较高——两者在权重和为 1 的约束下同时达到比较勉强。Phase 3 建议顺手验证这组示例数字是否真的存在对应的合法权重组合，而不仅仅是乘法验证通过就默认没问题。

### 9.4 下一步行动

1. **评审本方案**：确认架构、映射公式、阶段判定逻辑
2. **调整参数**：基于评审意见调整系数和阈值
3. **实现 Phase 1**：在 `wuxing_diagnose_v2.py` 基础上扩展 `diagnose_dao_realm()`
4. **验证 Phase 3**：用小禾/小石/孔子的模拟数据跑通流程

---

> **"道可道，非常道"** —— 本方案将"道"映射为可计算的坐标与参数，但坐标本身不是道，而是指向道的手指。融合的终极目标不是让机器"理解道"，而是让道境坐标系成为一个**可操作的导航工具**——帮助人看清自己"在哪里"，知道"往哪里走"。

---

## 附录 A：已知画像库

> 画像库用于"通"阶段判定。路径匹配任一画像即判定为"通"。当前版本包含 7 个画像（继承自五行诊断器 v2 `PATH_PROFILES`），Phase 3 将扩展至 12+ 个。

### A.1 画像清单

| # | 路径模式 | 画像名称 | 描述 | 经典案例 |
|---|----------|----------|------|----------|
| 1 | 木→火→土 | **趋势→热议→沉淀** | 新技术报道型：创新→传播→积累 | 科技媒体文章 |
| 2 | 火→水→金 | **热点→深挖→定论** | 深度调查型：传播→探索→批判 | 调查报道 |
| 3 | 土→金→水 | **综述→批判→开放** | 学术评论型：承载→批判→探索 | 学术论文 |
| 4 | 水→木→火 | **迷茫→破局→号召** | 思想启蒙型：探索→创新→传播 | 启蒙文献 |
| 5 | 土→水→金 | **传统→颠覆→精炼** | 范式翻转型：承载→探索→批判 | 库恩《科学革命的结构》 |
| 6 | 木→火→金 | **创新→传播→定论** | 科普型：创新→传播→批判收敛 | 科普文章 |
| 7 | 金→水→木 | **证据→探索→新方向** | 实证探索型：批判→探索→创新 | 实证研究论文 |

### A.2 画像匹配规则

```
匹配条件：路径前3个主导行 == 画像模式
优先级：精确匹配 > 子序列匹配
容错：允许路径长度 ≠ 3（取前3个主导行进行匹配）
```

### A.3 画像扩展计划（Phase 3）

| 计划新增画像 | 路径模式 | 适用场景 |
|-------------|----------|----------|
| 回归践履型 | 水→土→土 | 理论探索后回归实践 |
| 螺旋上升型 | 木→金→木 | 创新→批判→更高层次的创新 |
| 五行均衡型 | 五行动态均衡 | 成熟期，无单一主导行（如孔子"从心所欲"） |
| 逆境突围型 | 金→水→火 | 批判→探索→传播（小石的"恐龙域"路径） |
| 天人合一型 | 土→水→火 | 传统→探索→传播（如老子《道德经》的路径） |

### A.4 画像库版本

| 版本 | 日期 | 画像数 | 变更 |
|------|------|--------|------|
| v1.0 | 2026.07.28 | 7 | 继承自五行诊断器 v2 PATH_PROFILES |
| v1.1 (planned) | Phase 3 | 12+ | 新增逆境突围型、五行均衡型等 5 个画像 |